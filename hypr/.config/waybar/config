{
    // General Settings
    "layer": "top",
    "margin-bottom": 4,
    "spacing": 0,

    // Load Modules
    "include": [
        "~/.config/waybar/modules.json"
    ],

    // Modules Left
    "modules-left": [
        "hyprland/workspaces",
    ],

    // Modules Center
    "modules-center": [
        "custom/notification",
        "clock",
        "custom/exit",
    ],

    // Modules Right    
    "modules-right": [
        "tray",
        "power-profiles-daemon",
        "bluetooth",
        "network",
        "pulseaudio",
        "battery", 
    ]
}
