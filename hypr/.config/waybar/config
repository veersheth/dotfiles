{
    // General Settings
    "layer": "top",
    "spacing": 0,

    // Load Modules
    "include": [
        "~/.config/waybar/modules.json"
    ],

    // Modules Left
    "modules-left": [
        "hyprland/workspaces",
        "hyprland/window",
        "custom/media",
    ],

    // Modules Center
    "modules-center": [
        "custom/exit",
        "clock",
        "custom/notification",
    ],

    // Modules Right    
    "modules-right": [
        "tray",
        "power-profiles-daemon",
        "bluetooth",
        "network",
        "pulseaudio",
        "battery", 
    ]
}
