set -a terminal-features "tmux-256color:RGB"

bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux.conf reloaded."
set-option -g set-clipboard on
bind g neww -n "lazygit" lazygit
bind G run "~/scripts/github-webpage.sh"
bind E show-environment -g

set -g prefix C-SPACE
set -g base-index 1
set -g renumber-windows on   
set -g mode-keys vi
set -g mouse on

unbind -T copy-mode-vi Enter
bind-key -T copy-mode-vi Enter send -X copy-pipe-and-cancel "wl-copy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel "wl-copy"

#####################################################################################
# Status Bar Configuration
#####################################################################################

set-option -g status-position top
set-option -g status-style "bg=default,fg=white"
set -g status-justify absolute-centre

# Left side: Clear (Windows will start here)
set -g status-left ""

# Right side: [Folder Name] [Session Name]
set -g status-right "#[fg=#909090,bg=default] #{b:pane_current_path} #[fg=#ffffff,bg=#303030,bold] #S "
set -g status-right-length 100

# Window list (tabs) styling
# Active tab in pastel green
set-window-option -g window-status-current-style "bg=#303030,fg=#98e1a1,bold"
set-window-option -g window-status-current-format " #I:#W "

# Inactive tabs
set-window-option -g window-status-style "bg=default,fg=#909090"
set-window-option -g window-status-format " #I:#W "

#####################################################################################
# Navigation and Custom Scripts
#####################################################################################

bind-key -r C-f run-shell "tmux neww ~/scripts/tmux-sessionizer"
bind-key -r C-h switch-client -t "HOME"
bind-key -r C-t switch-client -t "TEMP"

bind -n 'M-\' run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys 'M-\\') || tmux select-pane -l"
bind M-l send-keys 'M-l'

#####################################################################################
# Vim-Tmux Navigation
#####################################################################################

vim_pattern='(\S+/)?g?\.?(view|l?n?vim?x?|fzf)(diff)?(-wrapped)?'
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +${vim_pattern}$'"

bind-key -n 'M-h' if-shell "$is_vim" 'send-keys M-h'  'select-pane -L'
bind-key -n 'M-j' if-shell "$is_vim" 'send-keys M-j'  'select-pane -D'
bind-key -n 'M-k' if-shell "$is_vim" 'send-keys M-k'  'select-pane -U'
bind-key -n 'M-l' if-shell "$is_vim" 'send-keys M-l'  'select-pane -R'

tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'

if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'M-h' select-pane -L
bind-key -T copy-mode-vi 'M-j' select-pane -D
bind-key -T copy-mode-vi 'M-k' select-pane -U
bind-key -T copy-mode-vi 'M-l' select-pane -R
bind-key -T copy-mode-vi 'M-\' select-pane -l
